#include <stdio.h>


int main() {

    int x = 5;
    int y = 10;
    int z = x + y;
    z = x - y;
    z = x * y;
    z = x / y;
    z = y % x;
    z *= 5;
    z += 5;
    z++;

    x = 5;
    y = 10;
    //printf('%d\n', x < y);
    //printf('%d\n', x > y);

    // Bitwise and
    //printf('%d\n', 20 & 23);


    return 0;
}

